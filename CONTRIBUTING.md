# مشارکت در Woo Factor

از مشارکت شما استقبال می کنیم.

## شروع سریع

1. ریپو را fork کنید و یک شاخه از `main` بسازید.
2. تغییر را کوچک و متمرکز نگه دارید.
3. افزونه را در یک WordPress + WooCommerce واقعی فعال و بررسی کنید.
4. lint پایه PHP را اجرا کنید:

```bash
for f in $(git ls-files '*.php'); do php -l "$f" || exit 1; done
```

5. Pull Request باز کنید و توضیح دهید چه تغییری داده اید و چگونه آن را تست کرده اید.

## اصول توسعه

- با APIهای WordPress و WooCommerce سازگار بمانید.
- همه خروجی های HTML را escape و همه ورودی ها را sanitize کنید.
- برای درخواست های حساس از nonce و capability مناسب استفاده کنید.
- RTL، فونت و شکست خطوط فارسی را در نمایش دسکتاپ و موبایل تست کنید.
- اطلاعات واقعی مشتری، سفارش، رمز، token یا کلید API را commit نکنید.
- تغییرهای وابسته به ظاهر فاکتور را با داده های سفارش کوتاه و بلند آزمایش کنید.

## گزارش باگ

برای باگ های عادی از Issue استفاده کنید. لطفاً نسخه WordPress، WooCommerce، PHP، مراحل بازتولید و نتیجه مورد انتظار را بنویسید؛ اما اطلاعات شخصی مشتری یا داده های فروشگاه را حذف کنید.

برای آسیب پذیری امنیتی، Issue عمومی نسازید و [SECURITY.md](SECURITY.md) را دنبال کنید.
